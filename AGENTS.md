# My Blocks Launcher 作業ルール

## 対象と基本方針

- このフォルダだけを独立したWordPressプラグインとして扱います。
- 最初に既存構成とプラグインヘッダーを確認し、依頼されていない機能変更は行いません。
- 大きな一括変更を避け、目的ごとに小さく変更します。
- WordPress標準APIを優先し、入力のサニタイズ、出力のエスケープ、必要な権限・nonce確認を行います。
- 変更後は、変更ファイル、確認結果、WordPress上の手動確認手順を説明します。
- 本番サイトへの直接反映や、依頼されていない公開作業は行いません。

## 通常の開発

通常の機能追加、不具合修正、コード整理では、原則として次の順で作業します。

1. ローカルコードを修正します。
2. 対象に応じた構文検査と動作確認を行います。
3. `build-release.ps1`でWordPress配布用ZIPを生成します。
4. ZIPの内容と構造を簡易確認します。
5. `git status`と差分を確認します。
6. ユーザーからGit操作の指示がある場合は、内容に合ったcommitを行います。
7. ユーザーからpushの指示がある場合は、現在の作業ブランチをGitHubへpushします。

コード変更後は、作業完了時点で`release`フォルダ内の現在Versionの配布ZIPも最新状態にします。同じVersionのZIPが存在し、コード変更を反映するために置き換える必要がある場合は`build-release.ps1 -Force`を使用します。

ユーザーから正式配布の指示がない限り、次は行いません。

- Version変更
- Tag作成
- GitHub Release作成
- Pull Request作成
- `main`へのマージ

## バックアップ

ユーザーが「バックアップして」と指示した場合は、次を行います。

1. 現在の変更内容を確認します。
2. PHP、JavaScript、JSONなど、対象に応じた構文検査を行います。
3. 機密情報、不要ファイル、ZIP、バックアップファイルが含まれていないことを確認します。
4. `git add`を行います。
5. 内容に合ったコミットメッセージで`git commit`を行います。
6. 現在の作業ブランチをGitHubへpushします。
7. push後のブランチ、コミットID、GitHubとの同期状態を報告します。

バックアップでは次を行いません。

- Version変更
- Pull Request作成
- `main`へのマージ

バックアップ対象にコード変更が含まれる場合は、構文検査後に配布ZIPも最新化します。ただし、Versionは変更しません。

## リリース

ユーザーが「リリースして」と指示した場合は、次を行います。

1. 現在の変更内容を確認します。
2. PHP、JavaScript、JSON、PowerShellなど、対象に応じた構文検査を行います。
3. WordPress上で確認すべき項目を整理します。
4. 次のVersionを確認します。
   - ユーザーがVersionを指定した場合は、そのVersionを使います。
   - 指定がない場合は、変更内容に応じたVersion案を提示し、勝手に変更しません。
5. プラグイン本体のVersionを確定し、Versionを記載する配布用ファイルがほかにも存在する場合は一致させます。
6. `build-release.ps1`を実行します。
7. `release`フォルダへ配布ZIPを生成します。
8. ZIP内を検証します。
   - 正しい最上位フォルダ名であること。
   - 必須ファイルが含まれていること。
   - `.git`、`AGENTS.md`、`PROJECT-BRIEF.md`、`build-release.ps1`などの開発用ファイルが含まれていないこと。
   - 二重フォルダ構造になっていないこと。
9. `git add`を行います。
10. 内容に合ったコミットメッセージで`git commit`を行います。
11. 現在の作業ブランチをGitHubへpushします。
12. Pull Requestを作成します。
13. リリース結果を報告します。

リリース結果には次を含めます。

- プラグイン名
- Version
- 実施した検査
- 生成ZIPの場所
- コミットID
- push先ブランチ
- Pull Request番号とURL
- `main`が未マージであること
- WordPressで確認すべき項目

リリースでは、ユーザーから明示的な許可がない限り`main`へマージしません。

## Git運用

- `main`は公開可能な安定版として扱います。
- 通常の変更は作業ブランチで行います。
- `main`へ直接commitしません。
- Pull Requestを経由して`main`へ統合します。
- pushやPull Request作成前に`git status`と差分を確認します。
- ユーザーの明示的な指示なしにブランチを削除しません。
- GitHubへの接続、push、Pull Request作成は、ユーザーから対象操作の明示的な指示がある場合に限ります。

## 配布物

- `release`フォルダとZIPはGit管理しません。
- 配布ZIPは`build-release.ps1`で生成します。
- コード変更後は、作業完了時に現在Versionの配布ZIPを最新状態にします。
- 既存ZIPは通常上書きしません。
- 同じVersionの変更を反映するため上書きが必要な場合のみ`-Force`を使用します。
- 配布ZIPの構造検証に失敗した場合は、リリース処理を停止します。
- 配布ZIPにはWordPress実行時に必要なファイルだけを含め、`.git`、`docs`、`release`、Git設定、作業ルール、ビルドスクリプトなどの開発用ファイルを含めません。
